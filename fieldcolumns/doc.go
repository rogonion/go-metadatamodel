/*
Package fieldcolumns is a module for working with a metadata model as an ordered list of fields (columns in a table).

It can perform the following tasks:
  - Extracts field properties into an ordered slice of fields (resembling columns in a table).
  - Reposition columns in rows of data.
*/
package fieldcolumns
